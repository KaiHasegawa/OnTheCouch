<%= @review.body %>
      <%#= link_to "削除",(@review.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>

<%= form_for([@review.item, @review, @comment]) do |f| %>
			<%= f.text_field :content %>
			<br>
			<%= f.submit 'コメントする' %>
<% end %>

	    <% @comments.each do |c| %>
	    	<%= c.content %>
	    	<%= c.end_user.name %>
	    <% end %>